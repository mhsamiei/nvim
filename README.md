# nvim

# For python

## install lsp python

`npm install -g basedpyright`

`pipx install ruff` or `python3 -m pip install ruff`

## install vertual env

```
mkdir -p ~/.local/share/nvim
python3 -m venv ~/.local/share/nvim/python-debug
~/.local/share/nvim/python-debug/bin/pip install debugpy
```
